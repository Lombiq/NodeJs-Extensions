export default {
    // The following path may have to be adjusted to your directory structure.
    extends: './node_modules/nodejs-extensions/config/stylelint.config.lombiq-base.mjs',

    // Add custom rules and overrides here.
    rules: {
    },
};
